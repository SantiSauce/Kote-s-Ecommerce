<form>
  <h1>Reset Password</h1>
  <h3>Please write a new one</h3>
  <div class="inset">
    <p>
      <label for="password">NEW PASSWORD</label>
      <input type="password" name="password" id="password">
    </p>
  </div>
  <p class="p-container">
    <input type="submit" name="go" id="go" value="Send">
  </p>
</form>
    {{!-- <h1>Please, properly request de link to reset your password</h1> --}}


<script>

const email = {{email}}
const form = document.querySelector('form')
const passwordInput = document.getElementById('password')

const sendPassword = (password) => {
  fetch('/session/passwordReset', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ password, email})
  })
    .then(response => {
      if (response.ok) {
        console.log('Data enviada exitosamente!');
      } else {
        console.log(response)
        console.log('Ocurrió un error al enviar la data.');
      }
    })
    .catch(error => console.error(error));
};

form.addEventListener('submit', event => {
  event.preventDefault();
  const password = passwordInput.value;
  console.log(email)
  sendPassword(password);
});

</script>

<style>
    @import "compass/css3";

* { box-sizing: border-box; }

body {
	font-family: "HelveticaNeue-Light","Helvetica Neue Light","Helvetica Neue",Helvetica,Arial,"Lucida Grande",sans-serif;
  color:white;
  font-size:12px;
  background:rgb(19, 23, 32);
}

form {
  width:300px;
  margin:30px auto;
  border-radius:0.4em;
  border:1px solid #191919;
  overflow:hidden;
  position:relative;
  box-shadow: 0 5px 10px 5px rgba(0,0,0,0.2);
  margin-top: 50px;
  box-shadow: 0px 0px 8px -2px var(--body-text-color);
}

form:after {
  content:"";
  display:block;
  position:absolute;
  height:1px;
  width:100px;
  left:20%;
  background:linear-gradient(left, #111, #444, #b6b6b8, #444, #111);
  top:0;
}

form:before {
  display:block;
  position:absolute;
  width:8px;
  height:5px;
  border-radius:50%;
  left:34%;
  top:-7px;
  box-shadow: 0 0 6px 4px #fff;
}

.inset {
 	padding:20px; 
}

form h1 {
  font-size:18px;
  text-shadow:0 1px 0 black;
  text-align:center;
  padding:15px 0;
  position:relative;
}



label {
  display:block;
  padding-bottom:9px;
}

input[type=text],
input[type=password] {
 	width:100%;
  padding:8px 5px;
  background:linear-gradient(#e7eaf6, #e7eaf6)
  border:1px solid #222;
  box-shadow:
    0 1px 0 rgba(255,255,255,0.1);
  border-radius:0.3em;
  margin-bottom:20px;
}

label[for=remember]{
 	color:white;
  display:inline-block;
  padding-bottom:0;
  padding-top:5px;
}

input[type=checkbox] {
 	display:inline-block;
  vertical-align:top;
}

.p-container {
 	padding:0 20px 20px 20px; 
} 

.p-container:after {
 	clear:both;
  display:table;
  content:"";
}

.p-container span {
  display:block;
  float:left;
  padding-top:8px;
}

input[type=submit] {
 	padding:5px 20px;
  border:1px solid rgba(0,0,0,0.4);

  border-radius:0.3em;
  background:rgb(177, 135, 81);
  color:white;
  float:right;
  font-weight:bold;
  cursor:pointer;
}

input[type=submit]:hover {
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,0.3),
    inset 0 -10px 10px rgba(255,255,255,0.1);
}

input[type=text]:hover,
input[type=password]:hover,
label:hover ~ input[type=text],
label:hover ~ input[type=password] {
 	background:#27292c;
}
</style>